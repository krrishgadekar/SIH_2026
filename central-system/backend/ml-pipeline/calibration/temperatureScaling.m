% temperatureScaling.m — module index for Task 2.5 calibration functions.
%
% This file is intentionally a comment-only module index, not a callable
% function. In MATLAB, callable functions must have a filename matching
% their function name, so the two components of temperature scaling live in:
%
%   applyTemperature.m  — inference-time temperature application.
%                         Call: calibratedProbs = applyTemperature(probs, T)
%
%   fitTemperature.m    — fitting script (stub until trained model is ready).
%                         Run as a script once valLogits.mat and valLabels.mat
%                         are available from the teammate's training output.
%
% The placeholder temperature_v1.mat (T=1, no-op) is in:
%   ml-pipeline/models/temperature_v1.mat
%
% See models/README.md for the full model inventory and swap instructions.
